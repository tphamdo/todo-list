export default class Todo {
    constructor(title, descr, dueDate, priority, done) {
        this.title = title;
        this.descr = descr;
        this.dueDate = dueDate;
        this.priority = priority;
        this.done = done;
    }
}
